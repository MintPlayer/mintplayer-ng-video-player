# Don't download latest LTS node, but use Node 14 instead
npm install -g @angular/cli@latest
npm install -g nx@latest
npx create-nx-workspace
> mintplayer-ng-video-player
> angular
> ng-video-player-demo
> SASS
> No
cd mintplayer-ng-video-player
# Remove the library folders
# Remove the application folders
# Open the angular.json, remove all projects, set "version":2
nx g @nrwl/angular:lib mintplayer-ng-video-player --buildable --publishable --import-path @mintplayer/ng-video-player
nx g @nrwl/angular:application ng-video-player-demo --routing --style=scss
code .